"""Interview Intelligence System (Phase 8): session lifecycle, the
backend-owned state machine and adaptive decision engine, structured
interview memory, deterministic readiness calculation, and report
generation. AI (via the centralized Phase 4 architecture) evaluates
answers, recommends actions, and writes narrative - the backend owns
every state transition, every number, and the final next question."""
